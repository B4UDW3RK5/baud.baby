No matching processes were found
cat@baudBaby:/usr/local/sbin % sudo /etc/rc.d/inetd restart
Cannot 'restart' inetd. Set inetd_enable to YES in /etc/rc.conf or use 'onerestart' instead of 'restart'.
cat@baudBaby:/usr/local/sbin % sudo /etc/rc.d/inetd onerestart
inetd not running? (check /var/run/inetd.pid).
Starting inetd.
cat@baudBaby:/usr/local/sbin %

----

cat@baudBaby:~/geomyidae % make ; sudo make install
cc -o geomyidae main.o ind.o handlr.o -L/usr/lib -L.
Password:
mkdir -p "/usr/local/bin"
cp -f geomyidae "/usr/local/bin"
chmod 755 "/usr/local/bin/geomyidae"
mkdir -p "/usr/local/share/man/man8"
cp -f geomyidae.8 "/usr/local/share/man/man8"
chmod 644 "/usr/local/share/man/man8/geomyidae.8"
cat@baudBaby:~/geomyidae %

-----

sudo cat /usr/local/etc/letsencrypt/live/baud.baby/{fullchain,privkey}.pem > ~/.config/weechat/ssl/relay.pem

-----

pfctl -F all -f /etc/pf.conf

-----

cp digitd.pl /usr/local/sbin/digitd
mkdir -p /etc/digitd
cp scripts/* /etc/digitd